import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  appLogo: {
    fontFamily: "Arial",
    letterSpacing: "1px",
    fontWeight: "bold",
    color: "#3f51b5",
    cursor: "pointer",
  },
}));
